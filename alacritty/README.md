# Alacritty config
Note that the colors are coming from `wpgtk`, and thus are not really supposed
to be edited. You can find them in `~/.config/wpg/templates`, or wherever you
like to keep your wpg templates.
