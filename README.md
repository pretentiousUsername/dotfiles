# My dotfiles
This is a big collection of the dotfiles I use. It doesn't include my neovim,
i3, or nixos configs, since I decided to put those in separate repos.

**Note to self.** If you need to add something from another git repository (such
as in `rofi_apps`), you should add them as a submodule—I'm a pro at software.
